platform :ios, '9.0'

# Comment the next line if you don't want to use dynamic frameworks
use_frameworks!

inhibit_all_warnings!

abstract_target 'main' do
  pod 'Resolver', '~>1.0.7'

  target 'TaschEngine'
  target 'TaschEngineTests'
end
